[ ] Platform commands should be visible only on supported platform.
[ ] Use highter resolution for app icon. It's blurry in Windows start menu.
[ ] LSP for proj files.
[ ] Scroll + Ctrl for font resize.
[x] Metrics should be regular struct, there is no benefit from having it as an array. It's actually just more complicated.
[x] All commands must have help text.
[ ] When opening file without project loaded change cwd?
[ ] Improve search sorting by pushing project local files to front?
[ ] Improve save dialog.
[x] Keybindings do not keep order from config file.
[x] Broken wrap for non ASCII chars:

 Now I can see the bug clearly. In  get_character_count_to_fit , the  num  counter tracks the number of characters (Unicode codepoints), but when used as a byte offset in  slice_range 
  at line 688, it's used as a byte index. Multi-byte UTF-8 characters (like  áčď…  which take 2+ bytes each) cause  num  to advance by 1 per codepoint but  converted_bytes  advances by
  their actual byte count and the final  return num  returns the character count, not the byte offs.



[ ] New UI
	[x] Resize list based on available items.
	[x] Command help should be on second line.
	[x] Pretty shortcut rendering.
	[x] Search in file should not use fullscreen overlay.
    [ ] Split resize.
    [x] Remove themes which are not looking good with a new setup.
	[x] Use custom line spacing for list entries.
